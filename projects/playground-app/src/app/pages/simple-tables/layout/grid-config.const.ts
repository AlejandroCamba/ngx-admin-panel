import { Grid } from '@ngx-admin-panel/core';

export const GRID_CONFIG = {
        contentPosition: {
            grid: {
                gridConfig: {
                    rowConfig: [
                        {
                            defaultSize: 5
                        }
                    ]
                },
                height: 'match-parent'
            }
        }
};
